# Miglore OS — Compose 环境变量模板
# 复制为 .env 并填入真实值 (根 .env 已被 .gitignore 排除, 不入库)

# MySQL (compose 内部 mysql 服务专用, 与生产 miglore 无关)
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_USER=miglore_os
MYSQL_PASSWORD=change_me

# Grafana (开发专用账号, 非生产)
GRAFANA_ADMIN_PASSWORD=change_me_grafana
